# =============================================================================
# Habits App Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to version control!
# =============================================================================

# =============================================================================
# SUPABASE CONFIGURATION
# Get these from your Supabase project settings: Settings > API
# =============================================================================

# Supabase Project URL
VITE_SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase Anonymous/Public Key (safe to expose in frontend)
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================

# Application URL (used by Edge Functions for redirect URLs)
# For local development: http://localhost:5173
# For production: https://your-domain.com
VITE_APP_URL=http://localhost:5173

# =============================================================================
# STRIPE CONFIGURATION (for Edge Functions)
# Get these from your Stripe Dashboard: https://dashboard.stripe.com/apikeys
# =============================================================================

# Stripe Secret Key (sk_test_... for test mode, sk_live_... for production)
# NEVER expose this in the frontend!
STRIPE_SECRET_KEY=sk_test_...

# Stripe Publishable Key (for frontend - safe to expose)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Stripe Webhook Signing Secret
# Get this when you create a webhook endpoint in Stripe Dashboard:
# Developers > Webhooks > Add endpoint > Signing secret
STRIPE_WEBHOOK_SECRET=whsec_...

# =============================================================================
# STRIPE PRICE IDS
# Create these in Stripe Dashboard: Products > Add product
# =============================================================================

# Pro monthly subscription price ID ($9/month with 7-day trial)
VITE_STRIPE_PRICE_ID_PRO_MONTHLY=price_...

# Founding membership one-time price ID ($149 lifetime access)
VITE_STRIPE_PRICE_ID_FOUNDING_ONE_TIME=price_...

# Legacy price IDs (kept for backward compatibility)
VITE_STRIPE_PRICE_MONTHLY=price_...
VITE_STRIPE_PRICE_ANNUAL=price_...

# =============================================================================
# SUPABASE EDGE FUNCTION SECRETS
# These are set via Supabase CLI, not in .env files:
#
# For local development:
#   supabase secrets set STRIPE_SECRET_KEY=sk_test_...
#   supabase secrets set STRIPE_WEBHOOK_SECRET=whsec_...
#   supabase secrets set STRIPE_PRICE_ID_PRO_MONTHLY=price_...
#   supabase secrets set STRIPE_PRICE_ID_FOUNDING_ONE_TIME=price_...
#   supabase secrets set APP_URL=http://localhost:5173
#
# For production:
#   supabase secrets set STRIPE_SECRET_KEY=sk_live_...
#   supabase secrets set STRIPE_WEBHOOK_SECRET=whsec_...
#   supabase secrets set STRIPE_PRICE_ID_PRO_MONTHLY=price_...
#   supabase secrets set STRIPE_PRICE_ID_FOUNDING_ONE_TIME=price_...
#   supabase secrets set APP_URL=https://your-domain.com
#
# SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY are automatically available
# =============================================================================

# =============================================================================
# LOCAL DEVELOPMENT NOTES
# =============================================================================
#
# 1. Start Supabase locally:
#    supabase start
#
# 2. Run Edge Functions locally:
#    supabase functions serve
#
# 3. Test Stripe webhooks locally with Stripe CLI:
#    stripe listen --forward-to localhost:54321/functions/v1/stripe-webhook
#
# 4. The Stripe CLI will give you a webhook signing secret for local testing
#    (different from your production webhook secret)
#
# =============================================================================
